// Default empty vocabulary. Add your own words via the in-app "+" button or CSV import.
// To preload words locally, create a separate `vocab-data.local.js` (gitignored)
// that sets `window.VOCAB_DATA = [...]` — it will be loaded after this and override.
window.VOCAB_DATA = window.VOCAB_DATA || [];
